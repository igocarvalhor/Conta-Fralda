create table if not exists public.records (
  id bigint generated by default as identity primary key,
  user_id uuid not null references auth.users(id) on delete cascade,
  date date not null,
  type text not null check (type in ('xixi', 'coco', 'ambos')),
  size text not null default 'M' check (size in ('RN', 'P', 'M', 'G', 'XG', 'XXG')),
  quantity integer not null check (quantity > 0),
  created_at timestamptz not null default now()
);

alter table public.records
add column if not exists user_id uuid references auth.users(id) on delete cascade;

create index if not exists idx_records_date on public.records(date);
create index if not exists idx_records_type on public.records(type);
create index if not exists idx_records_size on public.records(size);
create index if not exists idx_records_user_id on public.records(user_id);

create table if not exists public.profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  email text not null,
  phone text not null,
  phone_digits text not null,
  created_at timestamptz not null default now()
);

alter table public.profiles
add column if not exists email text;

alter table public.profiles
add column if not exists phone text;

alter table public.profiles
add column if not exists phone_digits text;

create unique index if not exists uq_profiles_email on public.profiles (lower(email));
create unique index if not exists uq_profiles_phone_digits on public.profiles (phone_digits);

-- Tabela de estoque de fraldas por tamanho
create table if not exists public.stock (
  id bigint generated by default as identity primary key,
  user_id uuid not null references auth.users(id) on delete cascade,
  size text not null check (size in ('RN', 'P', 'M', 'G', 'XG', 'XXG')),
  quantity integer not null default 0 check (quantity >= 0),
  low_threshold integer not null default 5 check (low_threshold >= 0),
  updated_at timestamptz not null default now()
);

create unique index if not exists uq_stock_user_size on public.stock(user_id, size);
create index if not exists idx_stock_user_id on public.stock(user_id);
